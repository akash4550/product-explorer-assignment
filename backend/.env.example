# --- Application Settings ---
PORT=3000
NODE_ENV=development

# --- Database Configuration (Optional: Defaults to SQLite if strictly needed) ---
# Uncomment these to use PostgreSQL instead of SQLite
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=product_explorer

# --- Scraper Configuration ---
# Render/Docker automatically sets this, but for local dev you might need it
# PLAYWRIGHT_BROWSERS_PATH=0